import XCTest

class TestCase: XCTestCase {
    func test_something() {
        let value = obj?.property!.anotherProperty!
        doSomething(optionalValue!)
        let dict_value = dict["key"]!
    }
}
